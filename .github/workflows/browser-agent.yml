name: Browser Agent - Bing Search Monitor

on:
  # Run every hour
  schedule:
    - cron: '0 * * * *'
  # Allow manual triggering
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-browser-agent:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Install Playwright browsers
      run: |
        python -m playwright install chromium
        python -m playwright install-deps chromium
    
    - name: Run browser-use agent
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: |
        python browser_agent.py
    
    - name: Commit and push results
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        # Only add JSON result files that were just created
        RESULT_FILES=$(find . -maxdepth 1 -name "results_*.json" -type f -mmin -5)
        if [ -n "$RESULT_FILES" ]; then
          echo "$RESULT_FILES" | xargs git add
          git commit -m "Add browser agent results - $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          git push
        else
          echo "No result files found to commit"
        fi
